{
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/next"
        }
    ],
    "functions": {
        "api/**/*": {
            "memory": 1024
        }
    },
    "buildCommand": "npm run prisma:generate && next build",
    "installCommand": "npm install"
}
